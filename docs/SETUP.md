# Local Setup

1. Create and activate a Python virtual environment.
2. Install dependencies with `python -m pip install -r requirements.txt`.
3. Keep the required model, task, and audio assets in the project directory.
4. Start the application with `python app.py`.
5. Allow camera access when Windows asks for permission.

Press `Q` in the monitoring window to exit the application.
